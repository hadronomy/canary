===============================================================================
 CANARY IS RUNNING WITH AUTHORIZATION DISABLED

 Protected REST and MCP routes will not require bearer tokens.
 Requests can reach the API without OAuth validation, scope checks, or principals.

 Use this only for local experiments or tightly isolated test fixtures.
 For normal testing, prefer a local OAuth issuer and short-lived test tokens.
===============================================================================
