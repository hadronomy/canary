# Auth contract. The web process supplies these values at run time.
#
# @import(../../.env.schema, pick=[BETTER_AUTH_SECRET, BETTER_AUTH_URL, CANARY_MCP_URL, CORS_ORIGIN])
# @generateTsTypes(path=./src/env.ts, exposeEnv=local)
# @defaultRequired=infer
# @defaultSensitive=false
# ---

# @type=enum(development, test, build, production)
APP_ENV=development
