/**
 * The replica cache contracts and Drizzle migration provenance for this build.
 *
 * Import this build-time diagnostic data from `@canary/db/replica/manifest`.
 * Runtime replica imports use only the compact cache-version map.
 *
 * @category models
 * @since 0.0.0
 */
export { manifest } from '#replica/manifest.generated';
