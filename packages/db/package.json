{
  "name": "@canary/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "db:generate": "varlock codegen && varlock run -- drizzle-kit generate",
    "db:migrate": "varlock codegen && varlock run -- drizzle-kit migrate",
    "db:push": "varlock codegen && varlock run -- drizzle-kit push",
    "db:studio": "varlock codegen && varlock run -- drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "pg": "^8.22.0",
    "varlock": "1.19.0"
  },
  "devDependencies": {
    "@canary/config": "workspace:*",
    "@types/pg": "^8.20.0",
    "drizzle-kit": "^0.31.10",
    "typescript": "catalog:"
  }
}
