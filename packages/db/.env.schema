# Drizzle process contract.
#
# @import(../../.env.schema, pick=[DB_HOST, DB_USERNAME, DB_PORT, DB_PASSWORD, DATABASE_URL])
# @currentEnv=$APP_ENV
# @generateTsTypes(path=./src/env.ts, exposeEnv=local, auto=false)
# @defaultRequired=infer
# @defaultSensitive=false
# ---

# @type=enum(development, test, build, production)
APP_ENV=development
