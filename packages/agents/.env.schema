# Agent contract. The web process supplies these values at run time.
#
# @import(../../.env.schema, pick=[DB_HOST, DB_USERNAME, DB_PORT, DB_PASSWORD, DATABASE_URL, BETTER_AUTH_URL, AGENT_MODEL, OPENROUTER_API_KEY])
# @generateTsTypes(path=./src/env.ts, exposeEnv=local)
# @defaultRequired=infer
# @defaultSensitive=false
# ---
