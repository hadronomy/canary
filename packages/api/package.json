{
  "name": "@canary/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "bun run --cwd ../.. env:codegen && APP_ENV=test varlock run -- vitest run",
    "models:generate": "bun scripts/models.ts && oxfmt --write src/catalog.generated.ts"
  },
  "dependencies": {
    "@canary/agents": "workspace:*",
    "@canary/auth": "workspace:*",
    "@canary/db": "workspace:*",
    "@orpc/server": "^1.14.6",
    "effect": "^4.0.0-rc.115",
    "varlock": "1.19.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@canary/config": "workspace:*",
    "@effect/vitest": "4.0.0-rc.115",
    "typescript": "catalog:",
    "vitest": "5.0.0"
  }
}
